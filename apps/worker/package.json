{
  "name": "@observed/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The Observed worker. Owns the wallet, the AskBots key and every paid call.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@observed/shared-types": "*",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.2"
  }
}
